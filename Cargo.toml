[package]
name = "sugarscape"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = "0.3"
rand = "0.8"
toml = "0.5"

[target.'cfg(target_arch = "wasm32")'.dependencies]
getrandom = { version = "0.2", features = ["js"] }
